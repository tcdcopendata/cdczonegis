var tabSummary = require('./tabSummary');
var tabCase = require('./tabCase');
var tabCluster = require('./tabCluster');
var tabContact = require('./tabContact');

var tab = {
    tabSummary,
    tabCase,
    tabCluster,
    tabContact
};

module.exports = tab;