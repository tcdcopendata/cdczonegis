var mapCase = require('./mapCase');
var mapCluster = require('./mapCluster');
var mapContact = require('./mapContact');

var map = {
    mapCase,
    mapCluster,
    mapContact
};

module.exports = map;